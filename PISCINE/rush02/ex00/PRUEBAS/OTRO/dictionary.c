#include "dictionary.h"
#include <stdlib.h>
#include <stdio.h>

t_dict *new_dict (void)
{
	t_dict	*tab;

	tab = malloc(sizeof(t_dict)*41);
//	Dict *D = (Dict *) malloc (sizeof (Dict));
//	D -> pairs = NULL;
//	D -> size = 0;
	return D;
}
